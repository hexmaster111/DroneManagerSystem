<UserControl x:Class="GraphicalConsole.BaseUcs.DroneIdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GraphicalConsole.BaseUcs"
             xmlns:genericTypes="clr-namespace:DroneManager.Interface.GenericTypes;assembly=DroneManager.Interface"
             xmlns:graphicalConsole="clr-namespace:GraphicalConsole"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="5" BorderBrush="LightGray">
        <Grid>
            <!-- <Grid.DataContext> -->
            <!--     <genericTypes:DroneId/> -->
            <!-- </Grid.DataContext> -->
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Drone"/>
            <TextBlock Grid.Column="1" Text="{Binding IdString }"/>
        </Grid>
    </Border>
</UserControl>